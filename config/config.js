/*global require */
// Set the require.js configuration for your application.
require.config({

    baseUrl : 'src',

    deps: ["../config/main"],

    paths: {
    },

    shim: {
        // Put shims here.
    }
});
